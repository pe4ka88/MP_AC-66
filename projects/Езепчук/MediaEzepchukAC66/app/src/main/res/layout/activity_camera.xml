<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Фоновое изображение -->
    <ImageView
        android:id="@+id/bgImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/bg_media2"
        android:scaleType="centerCrop"
        android:contentDescription="Фон"/>

    <!-- Полупрозрачный оверлей для контраста -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#66000000"/>

    <!-- Основной контейнер -->
    <LinearLayout
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:padding="24dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Заголовок -->
        <TextView
            android:id="@+id/titleText"
            android:text="Камера"
            android:textSize="26sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"
            android:layout_marginBottom="24dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <!-- Карточка с фото и кнопкой -->
        <LinearLayout
            android:id="@+id/cardContainer"
            android:orientation="vertical"
            android:background="@drawable/card_background_2"
            android:padding="20dp"
            android:elevation="8dp"
            android:clipToOutline="true"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <!-- Превью фото (скрыто до съемки) -->
            <FrameLayout
                android:id="@+id/previewContainer"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:layout_marginBottom="16dp"
                android:visibility="gone">

                <ImageView
                    android:id="@+id/imageView"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="centerCrop"
                    android:background="#EEEEEE"
                    android:clipToOutline="true"/>

                <!-- Затемнение под текст -->
                <View
                    android:layout_width="match_parent"
                    android:layout_height="45dp"
                    android:layout_gravity="bottom"
                    android:background="@drawable/text_gradient"/>
            </FrameLayout>

            <!-- Кнопка камеры -->
            <ImageButton
                android:id="@+id/btnTakePhoto"
                android:layout_width="65dp"
                android:layout_height="70dp"
                android:layout_gravity="center"
                android:background="@drawable/button_image_background_4"
                android:contentDescription="Сделать фото"
                android:scaleType="center"
                android:src="@android:drawable/ic_menu_camera" />

        </LinearLayout>

    </LinearLayout>
    <TextView
        android:id="@+id/copyrightText"
        android:text="© 2026 Езепчук А.С. АС-66"
        android:textSize="12sp"
        android:textColor="#DDFFFFFF"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="12dp"
        android:alpha="0.8"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"/>
</RelativeLayout>